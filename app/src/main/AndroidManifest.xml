<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.drivinglicence">

    <!-- Quyền hệ thống -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Exact alarm (Android 12+) - Cần thiết để báo thức chạy đúng giờ chính xác -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- QUYỀN QUAN TRỌNG CẦN THÊM (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- FCM -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <application
        android:name=".DrivingLicenseApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@drawable/driving"
        android:roundIcon="@drawable/driving"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DrivingLicense"
        tools:targetApi="31">

        <!-- Activities -->
        <activity android:name=".app.activites.CommonMistakesActivity" android:exported="false"/>
        <activity android:name=".app.activites.CountDownTestActivity" android:exported="false"/>
        <activity android:name=".app.activites.LessonViewPagerActivity" android:exported="false"/>
        <activity android:name=".app.activites.SearchLawActivity" android:exported="false"/>
        <activity android:name=".app.activites.TipsActivity" android:exported="false"/>
        <activity android:name=".app.activites.RoadTrafficSignsActivity" android:exported="false"/>
        <activity android:name=".app.activites.LearningTheoryActivity" android:exported="false"/>
        <activity android:name=".app.activites.TestLicenseActivity" android:exported="false"/>

        <activity android:name=".app.activites.HomeActivity" android:exported="true" />

        <activity android:name=".app.activites.SplashActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- DAILY REMINDER RECEIVER -->
        <receiver
            android:name=".receiver.DailyReminderReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Boot Receiver để khởi động lại alarm khi device reboot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Notification Config -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/colorAccent" />

    </application>

</manifest>
